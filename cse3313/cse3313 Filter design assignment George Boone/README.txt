Run the matlab code to play the filtered audio.

The matlab code attempts to save the filtered audio but fails, I believe its
a driver issue on my end.

matlab error: "unsupported SampleRate value" by audiowrite(...) , im passing it the exact values audioread(...) returns.